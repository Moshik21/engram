"""Write-side observer/reflect synthesis helpers.

Deterministic clustering, importance gating, and observation synthesis used by
the ``reflect`` consolidation phase. Ships dark: nothing here runs unless
``observer_reflect_enabled`` is True (the phase guards before importing).
"""
