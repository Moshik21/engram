# Engram state assessment — 2026-09-04
Produced by a 16-agent workflow (6 readers, 3 refuters, 3 plans, 3 judges, 1 synthesis; 2.67M subagent tokens, 497 tool uses) after the resident-agent plan landed (`2b9ddbf`..`e3eae92`). Readers worked read-only against the repo and the live REST API; no second process opened the store. Judges disagreed on the winner (see the last section); this is the synthesis, with its disagreements kept visible per STANDING_GOAL §2.
## Where we stand
- Engram the protocol, measured against a live helix-native shell (8,692 episodes / 953 entities / 1,761 edges exact, GET /api/storage): DELIVERED = agent-as-extractor (narrow-only factory, 2,589 proposal commits, 0 external extractor invocations; extraction/factory.py:20-22, ~/.engram/harness-metrics.json) and the claim_authority contract as a mechanism; PARTIAL = one brain per person (projects are a 0.6 multiplier + a tag, pipeline.py:562-624, never evaluated as topology), cross-agent continuity (connectors exist; value = 3-4/14 warm, 0/14 cold on unseen questions, 8/14 vs 4/14 untouched at 69k vs 13k chars, STANDING_GOAL.md:222,306), ACT-R (decay side wired; ranking side refuted, weights only reach an entity channel truncated to budget 0, config.py:956; pipeline.py:2757-2762), consolidation (a 2h mop runs 4 of 15 phases; 11 have no runner on the shipped config; 20/20 history cycles stuck 'running'), local-only (default path local; key-activated Gemini/Voyage egress, an Anthropic-only chat route and a key-demanding setup wizard remain); INERT = graph as memory structure (scoreBreakdown activation/spreading/edgeProximity 0.0 on every live item; kill rig KILL twice, GRAPH_THESIS.md §5); ABSENT = cross-device sync / federation (design-only by decision).
- The dogfood brain is two things glued together. Irreplaceable (~6,100 rows): ~5,550 real user/assistant turns across ten projects, 509 Codex summaries, 4 mcp/user_correction captures, 239 ent_ entities with 404 ent-ent edges, and the only behavioural signal ever earned (144 cues / 1,952 hits / 388 near-misses / 48 used, GET /api/stats cue_metrics). Disposable/regenerable (~2,600 rows + most of the graph): 695 auto:bootstrap snapshots, 1,033 tag-shaped harness machinery rows (all source auto:prompt; 29.9% of [user| turns), 587 session markers, 33 precompact, 249 probe rows; 546 art_ + 59 decision_statement + 2 schema entities carrying ~77% of edges; 6,813 of 7,615 cue nodes without text; 7,017 of 8,692 episode vectors without a readable payload; 1.18M ConsolDecisionTrace docs in the user-facing BM25 index (avgdl 70.7 vs 339.8).
- VERIFIED (this run): the 4,581 May-June turns exist nowhere but the LMDB env. Earliest Claude Code transcript is 2026-07-07 by mtime and in-file timestamp (~/.claude/projects, 1,033 jsonl); Codex sessions have 42 May / 5 June files; openapi has only GET /api/episodes (content[:200], graph.py:3166-3167) and no export/import/dump route. Plan 2's 'duplicated untruncated in transcripts' premise is false; all three judges caught it.
- The machine, not only the store, is a confound: swap 3,649/4,096 MB used right now (sysctl vm.swapusage), data.mdb 8,198,848,512 B, 50 GiB free; brain windows still died on 'find_entities_by_group timed out after 20.0s' on 09-04 (~/.engram/logs/engram-brain.log); every cold meter run fell to context_packet_fallback at 2.5-4.5 s (STANDING_GOAL.md:222,244). Nothing measured so far separates 'store too big' from 'machine too loaded' from 'the fallback cascade is code'.
- The store is NOT the cause of most of what is wrong. Store-independent: the graph consumer verdict (KILL on a planted corpus), the 11 runner-less phases, the entity channel at budget 0, the recent_observation >=3-token cache hijack (recall_surface.py:2751), the 2000-char silent truncation at capture (capture-response.sh:58; setup.py:861), machinery stored at capture with no is_machinery gate (capture_service.py:239-243; is_machinery callers are pipeline.py:602, index_completeness.py:1103, battery only), agent adoption (agentRecallCount 0), the ~5x character cost, and every external-model code path. A fresh brain fixes none of these.
- Code-side baggage after 4907143 is small and almost all dead-by-config: one live external-model path (POST /api/knowledge/chat -> chat_runtime.py:309 anthropic.AsyncAnthropic; its top-level import is why `anthropic` stays a base dep for `engram serve`), five dead Anthropic branches (hyde, triage judge/escalation, infer, merge, edge server adjudicator), the orphaned EntityExtractor body, Voyage/Gemini/Cohere providers, and `engram setup` still REQUIRING an Anthropic key (setup.py:120). FalkorDB/Redis/Postgres lanes are census trap-mapped (keep pending a Docker boot falsifier). Docs still describe Ollama/Haiku/Gemini as operation in ~30 REFERENCE.md lines, README:264,274, SKILL.md:7,82, install/helix.md, .env.example, website/src.
- Measurement integrity is the weakest layer: the 9/14 meter was self-reference; the brain contains this session's own rig analyses (STANDING_GOAL.md:249); meter noise has never been measured; harness-metrics were pytest-contaminated until c833c83; /api/stats reports 857 relationships (a 10-entity sample x N/10, graph.py:2676-2692) against 1,761 exact; eleven ledger rows are closed-in-code but unstruck; the ENOSPC blocker row (line 219) is falsified today (native store created 2/2 on scratch) with cause still unknown.
- Fresh-brain mechanics are cheap and reversible: the data dir is pinned in exactly one place (~/.engram/.env ENGRAM_HELIX__DATA_DIR, sourced by both LaunchAgents; MCP clients and hooks reach the shell over HTTP), the engine create_dir_all's a new path, and the old dir can be retained untouched (mtime as proof of no second open). Footguns: the installed ~/.local/bin/engramctl `setup` rewrites .env to standard/all/rework/worker=true (the Jetsam config, installed engramctl:357-403); an absent DATA_DIR opens the May-26 ~/.helix/engram-native and /health reports healthy; the SessionStart hook POSTs /api/knowledge/bootstrap unconditionally and the route ignores artifact_bootstrap_enabled, so a fresh brain is re-seeded with 695 bootstrap rows on first use; `engram captain export` double-opens LMDB with no lock (captain_cli.py:91-103).

## Brain verdict
**Fresh-after-export: switch to a fresh native store with UNCHANGED code and the old dir retained as the archive, but only after a shell-down, scratch-tested export of the ~6,100 irreplaceable rows has been verified — because the 4,581 May-June turns exist nowhere but the LMDB env (earliest Claude transcript 2026-07-07; /api/episodes caps content at 200 chars and no export route exists), so nothing can be blown away first.**

### Irreplaceable — export first
- Conversation rows (~6,100): auto:prompt + auto:response minus the strict machinery regex (~5,550), 509 codex/codex_*/auto:codex-probe summaries, 3 mcp + 1 user_correction, 13 claude-code + 3 claude_code + 1 cursor + 7 tool_piggyback — full content, source, project, conversation_date AND created_at (capture re-stamps created_at=utc_now, capture_service.py:84,131; recency reads conversation_date, pipeline.py:2303-2305, but listing order and any created_at consumer will shift)
- 239 ent_ entities and their 404 ent-ent edges with provenance (PART_OF 170, MENTIONED_WITH 147, DECIDED 14, SAVED_TO 5), the 4 short dec_ Decisions, Person/Organization/Social_account nodes, 17 real Projects — exported with 'Konnern Moshiere' corrected and the 9 'Engram' nodes folded on re-entry
- The identity_core set via find_identity_core_entities (schema.hx:273) — count UNKNOWN today; no GET route exposes it
- Cue feedback counters (144 cues / 1,952 hits / 388 near-misses / 48 used-episodes) and 127 entities with accessCount>0 + activation-snapshot entries — archived as JSONL for the record; no route can write them back, so they are re-earned, not restored
- The measurement baseline: all 44 pages of GET /api/episodes as a 200-char index, GET /api/stats, /api/storage, /api/lifecycle/summary, /api/hygiene/debt snapshots, and the two clean rigs as the OLD-brain bar (explicitly not the fresh brain's bar)
- A verified `engram backup create` + `verify` of the old dir (both Sep-3 backups exist; method='copy', so a new one costs 7.6 GB) — the only whole-brain rollback

### Regenerable — discard with the directory
- 695 auto:bootstrap episodes (all '[project-bootstrap|' headed; regenerable by bootstrap_project from the repos; artifactBootstrap already reports enabled=false)
- 546 art_ Artifact entities, 59 decision_statement dec_ blobs, 2 schema_ nodes and the ~1,326 edges touching them (PART_OF 657 + DOCUMENTED_IN 202 + INSTANCE_OF 123 + SUPERSEDED_BY 260) — the artifact index already reports them stale=true
- ~1,900 non-conversational rows: 1,033 strict-regex harness machinery (all auto:prompt), 587 auto:session markers, 33 claude:precompact, 249 probe/soak/gate rows
- 6,813 EpisodeCue nodes without cue_text and their vectors; 7,017 payload-less episode vector rows; 1.18M ConsolDecisionTrace docs (deletion needs a new HelixQL query and a consumer check — readers exist in audit_reader.py, learning.py:115, triage.py, phase_runner.py, presenter.py)
- 6 throwaway Project entities (engram-followup-fresh-*, engram-followup-test, engram-fresh-verify-59519), 8 duplicate 'Engram' Concepts, 4 of 5 'Cold Decision hit…' dupes, the misspelled 'Konnern Moshiere' Person (ent_dea20bfaa410)
- In-dir sidecars retired with the dir: packet-cache.sqlite3, cue-index-outbox.sqlite3 (3.7 MB pending index work), scheduler-state.sqlite3, and surfaced-usage.sqlite3 (after its counters are archived)
- ~/.engram sidecars moved aside, not deleted: activation-snapshot.json (phantom ent_* ids restored blindly for 14 days, activation.py:424-466), hygiene-state.json (index_consistency offsets episode=8000/chunk=1000 would skip a fresh brain's first rows), harness-metrics.json (pytest-contaminated before c833c83), brain-status.json (old brain's last mop reported as 'latest'); keep bm25-breaker-state.json (keyed by data-dir path) and ~/.engram/models/fastembed
- The 20 stale 'running' consolidation cycle rows (Jul-15..21) and, on disk, the manifest-less 17 GB Jul-16 backup (CLI-unrestorable, backup_cli.py:224-231) and the 835 MB May-20 ~/.helix/engram-native-dogfood — Konner's call, reclaimed either way

### Blocking unknowns
- Whether the hook/auto-observe capture path can carry `project` for the re-seed: ObserveBody has no project field (verified via openapi) and /api/knowledge/auto-observe declares no requestBody schema; without it the load-bearing project-prefix effect (7,7,7 vs 4,4,4) is lost on re-seed
- Root cause of the intermittent ENOSPC at native env creation (3/3 fail 07-24, 2/2 pass 09-04) — a 20 GiB map plus a 7.6 GB export copy must fit beside 33 GB of same-volume backups on 50 GiB free
- Whether the live shell binary carries the b2bf4cc arena-leak fix (needs make build-native + restart; not visible from REST) — decides whether label-scan leaks confound the 48 h machine probe
- identity_core membership and count (no GET route; only find_identity_core_entities via a shell-down script)
- Whether the index-consistency offset cursors wrap on a smaller brain (no wrap logic found in index_completeness.py / hygiene_ops.py:566-597)
- Whether Konner approves the three user-config edits the switch needs: one line in ~/.engram/.env, the bootstrap POST in ~/.engram/hooks/session-start.sh, and the machinery/truncation changes in capture-prompt.sh / capture-response.sh
- Whether the swap number on an empty store is attributable to Engram at all: shell RSS is ~1.3 GB of a 3.6 GB swap on a 16 GB Mac also running three claude processes, WebKit and ChatGPT (judge 1) — the 48 h probe must record other load
- The remember()/observe() split inside every non-auto source (MCP tools accept a caller-supplied source), and whether the 47 May-June Codex session files duplicate any of the 509 codex summaries

## Recommended plan (each step names its probe)
0. 0. Freeze the instrument before anything moves: on a quiet machine with no second store mapped, run the clean meter rig warm+cold twice back-to-back, the untouched battery once, 10 consecutive explicit recalls, and record shell RSS + sysctl vm.swapusage + other-process RSS. Dump GET /api/stats, /api/storage, /api/lifecycle/summary, /api/hygiene/debt and all 44 pages of GET /api/episodes into a dated ids file (id, source, header, status, project) and count the irreplaceable class in writing.
   - **probe:** Two meter runs agree within 1 question at 3-4/14 warm, 0 cold; battery 8/14 vs 4/14; ids file has 8,692+ rows with the irreplaceable set ≈6,100 named. Disagreement >1 question = swap regime: record it, do not tune, proceed (the fresh brain is the fix).
1. 1. Write and dry-run the export script on a SCRATCH native store (fresh reader proved creation works: HelixEngine + HelixGraphStore/NativeTransport in 0.03 s), then `engramctl stop`, confirm no shell pid and no engram-shell.lock holder, and run it read-only against the stopped live dir: full-content JSONL of the irreplaceable rows (content, source, project, conversation_date, created_at, status), the ent_ entities + ent-ent edges with provenance, identity_core via find_identity_core_entities, and the 802 cue rows with counters. Then `engram backup create` + `verify`. Never run captain export with the shell up.
   - **probe:** Row count = 8,692 − 695 − 587 − 33 − 249 − ≥1,033 ± captures since; ≥90% of the 6,121 rows the listing showed at exactly 200 chars come out longer; 20 random rows match listing prefixes; 404 ± ent-ent edges; identity_core count recorded (0 is a valid answer); verify passes with backup-manifest.json; ≥40 GiB still free. Any mismatch → stop; nothing else runs.
2. 2. Reconcile the ledger the same day: strike the eleven stale-closed rows (tickets 32, 33; rows 275, 276, 282, 283, 285, 286, 287, 293, 301; 269), restate line 219 as 'ENOSPC 3/3 on 07-24, 2/2 pass on 09-04, cause unknown, positive probe kept', update the §1 table to 8/14 vs 4/14 (09-04), and note the 857-vs-1,761 sampled relationship count.
   - **probe:** This probe cannot fail on its own (it verifies the edit was typed); its value is that the next agent is not sent after closed work. Falsifier for the ENOSPC restatement: HelixEngine(data_dir=<scratch>) on the day of the switch.
3. 3. Switch with UNCHANGED code (needs Konner's explicit OK for each user-config edit): free disk first (delete the manifest-less 17 GB Jul-16 backup and the 835 MB May-20 dir only after step 1 verify); mv activation-snapshot.json, hygiene-state.json, harness-metrics.json, brain-status.json to ~/.engram/retired-<stamp>/; edit exactly one line of ~/.engram/.env (ENGRAM_HELIX__DATA_DIR → new absolute path; never unset it, never run the installed `engramctl setup`); disable the bootstrap POST block in ~/.engram/hooks/session-start.sh for the measurement window; `engramctl start`.
   - **probe:** diff of ~/.engram/.env shows exactly one changed line; GET /api/storage paths[0].path == new dir, backend helix_native, counts all 0, countsStatus 'live'; GET /api/knowledge/runtime activation == {quiet, wave2, off}; GET /api/activation/snapshot empty; old dir mtime unchanged. After the first session: GET /api/episodes has 0 source=auto:bootstrap and entity_type_distribution has no 'Artifact'.
4. 4. Run the machine-vs-store falsifier on the EMPTY store for 48 h before concluding anything (Plan 2 step 7, every judge's steal): sample serve RSS, sysctl vm.swapusage with other-process RSS beside it, cold single-entity GET /api/entities/{id} latency, and grep engram-brain.log for 'timed out after 20.0s' across two LaunchAgent mop windows.
   - **probe:** PASS: swap < 1.5 GB at steady state with comparable desktop load, no timeout lines in two consecutive windows, cold entity GET < 200 ms, brain-status.json vector_backfill.missing_before.episodes == 0 and snapshot_sync.entities == 0. FAIL: swap stays > 3 GB or timeouts recur on an empty store → record 'machine, not store' in the ledger and revert the one .env line (old dir untouched).
5. 5. Re-seed the exported irreplaceable rows through the capture path in created_at order with source and conversation_date preserved (recency reads conversation_date, pipeline.py:2303-2397 — verified), and with project carried by whatever path accepts it (resolve the blocking unknown first: ObserveBody has no project field; if no route carries it, seed via a shell-down store-level write and say so). Then re-remember the corrected Person/Org/Decision facts via remember(proposed_*) and let the agent re-project rows it flags.
   - **probe:** counts.episodes == export rows; vectors missing == 0 (old: 7,021); cue coverage > 9.2%; items[].project non-null share == export share; conversation_date distribution matches the export; data.mdb and shell RSS recorded (expect ≪ 7.6 GB, < 2 GB). Project share below export share = the seed path lost project → stop and fix the path before measuring.
6. 6. Re-run step 0's instruments on the re-seeded store — the size hypothesis's own kill switch: same clean rig warm/cold split, same untouched battery, 10 explicit recalls, no second store mapped, hooks quiet.
   - **probe:** Warm ≥ step-0 baseline (3-4/14) and cold > 0/14 with no context_packet_fallback on cold runs; battery ≥ 8/14 at ≤ 69k chars. If cold is still 0/14 on a small non-swapping store, the cold problem is the fallback cascade / recent_observation hijack (code), not baggage — that redirects the next ticket to recall_surface.py:2751 and the cascade, not to the store.
7. 7. Close the write side so the noise does not come back (code + hook edits, Konner's OK for ~/.engram/hooks): a capture-time is_machinery gate on the UserPromptSubmit path (drop, or store-unindexed with a counter), a truncation marker + counter replacing the silent RESPONSE="${RESPONSE:0:2000}" cut, and a Python wrapper over the existing native delete_cue_vector query so cue demotion drops the vector instead of upserting cue_text=''.
   - **probe:** After one working day the newest 500 rows of GET /api/episodes contain 0 strict-regex machinery rows and 0 '[project-bootstrap|' rows; at least one captured response carries the truncation marker (proves the path fires) and no 2,000-char row lacks it; counts.cues == cue_metrics.cue_count; a newly demoted cue has no vector row by id.
8. 8. Fourteen days of organic use with no planting: every remember/observe carries proposed_entities/proposed_relationships; weekly measure relationship yield, extractedBy share, machinery capture rate (honestly labelled 'not fixed by the store' if step 7 has not landed), and mop window health.
   - **probe:** GET /api/stats projection_metrics.yield.avg_relationships_per_projected_episode > 0 and relationship_count > 0 (old: 0.0 over 4,045); extractedBy majority 'projected_agent' over 20 recalls; counts.relationships grows with counts.episodes; brain-status.json ok==true, duration < 30 s, no 'window closed early'. Yield still 0 at day 14 answers §7 Q3 (the resident agent is not proposing edges) and no store change can fix it.
9. 9. Day-14 gate written into STANDING_GOAL.md §8 beside the step-0 baseline: KEEP the fresh brain iff step 4 passed, step 6 cold ≠ 0, step 8 yield > 0, AND answers ≥ repo files at ≤ 2x the characters on a new unseen-question set written by a second agent from the transcripts (judge 3's missing bar); otherwise revert the one line and record which condition failed. Only after KEEP is the old dir's fate decided.
   - **probe:** The §8 entry cites all four probe values with REST paths; `engramctl status` shows the chosen dir; old dir mtime unchanged since the switch. Any condition unmeasured = the gate did not happen; the switch stays provisional.
10. 10. In parallel and independent of the brain decision (code only, its own PRs, each behind Konner's approval): fix setup.py to stop asking for/writing external keys and smoke-test the local embedder instead; decide the dashboard chat (route through the resident agent or drop it) and move `anthropic` to a benchmark extra; delete the five dead Anthropic branches, the EntityExtractor body, Voyage/Gemini, Cohere and the no-key guard; rewrite README/REFERENCE/SKILL/install docs/.env.example/website to the narrow-only, local-only truth; republish the clawhub skill.
   - **probe:** `engram serve` and `engram mcp` import with anthropic/voyageai/google-genai uninstalled; `engram setup` writes an .env with no key line; grep of server/engram for anthropic|voyage|gemini|cohere hits lexicon/comment lines only; doc grep for Haiku|Ollama|Voyage|17-phase returns only status-headed experiment records; `engram evaluate --smoke --mode helix` on a temp dir passes.
11. 11. Defer Plan 3's structural cuts (graph hot path, cold brain, narrow auto-projection, profile matrix, lite backend, dashboard panels) until steps 4-9 have produced numbers on the fresh brain; each cut then gets its own flag-off-first probe on the untouched meter, one mechanism class at a time, with the kill rig's arms preserved on a branch and Konner's explicit approval per deletion.
   - **probe:** For each mechanism: meter ≥ baseline with it OFF, 10 explicit recalls p50 not worse, and no 'lost question' — a lost question is a hidden consumer to name, and the mechanism stays until it is named. No code is deleted before its OFF-probe passes.

## What is left (45 rows, tier / kind)

| tier | kind | item | evidence |
|---|---|---|---|
| T0 | old-brain-baggage / measurement | Export gate: no bulk export exists; the ~6,100 irreplaceable rows (May-June 4,581 with no other copy), 239 ent_ entities, 404 ent-ent edges, identity_core set and cue counters can only leave the store via a shell-down read-only script tested on a scratch native store first | openapi: only GET /api/episodes, no export/import/dump; graph.py:3166-3167 content[:200]; captain_cli.py:22-36 name/summary only and :91-103 no lock; earliest ~/.claude/projects transcript 2026-07-07 (verified); schema.hx:273 find_identity_core_entities |
| T0 | measurement | Meter noise never measured; warm/cold split per run; self-reference (the brain holds this session's rig analyses) — two back-to-back clean-rig runs must agree within 1 question before any change is attributed | STANDING_GOAL.md:222,249; tests/rigs/recall_meter_rig_clean_2026-09.json; Plan 3 step 1 (all three judges name it best idea) |
| T0 | measurement | Machine vs store unresolved: swap 3,649/4,096 MB now, 20 s native timeouts killing brain runs, cold meter 0/14 via context_packet_fallback — any A/B with a second store mapped is invalid | sysctl vm.swapusage (this run); ~/.engram/logs/engram-brain.log 09-04 08:48 'find_entities_by_group timed out after 20.0s'; STANDING_GOAL.md:223,244 |
| T0 | measurement | ENOSPC blocker row is falsified as stated (fresh native store created 2/2 today, raw + product path) but cause unknown and intermittent; row must be restated and a positive creation probe kept in the runbook | STANDING_GOAL.md:219 (cited as ticket 35 at :273,507,584 but unnumbered); fresh reader scratchpad/freshtest_raw.py + freshtest_transport.py; 50 GiB free |
| T0 | measurement | Eleven ledger rows closed in code but unstruck (tickets 32, 33; rows 275, 276, 282, 283, 285, 286, 287, 293, 301; 269 config edit) plus the July §1 table superseded by :306 and the 390866b commit-message correction | STANDING_GOAL.md:34-37,217-218,269,275-276,282-287,293,301,329-332; spread_injection.py:43; runner.py:79-81; apply.py:342-351; config.py:526; usage_surface_store.py:79-90 |
| T0 | measurement | /api/stats, /api/lifecycle/summary and /api/graph/atlas report 857 relationships (10-entity sample x N/10) against 1,761 exact — dashboard understates the graph 2x | graph.py:2676-2692 sample_ents=entities[:10]; schema.hx:506-508 exact COUNT via GET /api/storage counts.relationships=1761 |
| T0 | measurement | Reranker records nothing when reranker_enabled=False (quiet forces it), so 'did nothing' and 'never armed' are indistinguishable; the 7229f89 rerank fix is byte-identical at shipped defaults | STANDING_GOAL.md:268; pipeline.py:2421; config.py:3283 _set('reranker_enabled', False) |
| T0 | measurement | Pre-09-04 harness-metrics figures are pytest-contaminated (leak closed by c833c83; historical numbers cannot be un-contaminated); stale pytest keys remain in ~/.engram/bm25-breaker-state.json; a 'harness:test' writer hit the live loop-adjustments.jsonl | STANDING_GOAL.md:285,239; harness_metrics.py:93-97; conftest.py:40-42; ~/.engram/loop-adjustments.jsonl 2026-09-04T16:33Z created_by='harness:test' |
| T1 | repair | recent_observation >=3-token cache hijack: the session's newest observe answers any later query sharing 3 tokens as cache_satisfied without running the pipeline (a 7-min-old capture hijacked a real question live) | recall_surface.py:2751 best_score >= 3; :2747 guard only bites when not is_graph; STANDING_GOAL.md:258; no commits to recall_surface.py since 09-03 |
| T1 | repair | Captured assistant responses silently cut at 2000 chars mid-word with no marker or counter (454 live rows at the wall) — write-side loss on the irreplaceable class; fresh brains inherit it | setup.py:861 RESPONSE="${RESPONSE:0:2000}"; ~/.engram/hooks/capture-response.sh:58 (user config, needs permission); STANDING_GOAL.md:255 |
| T1 | repair | Installed ~/.local/bin/engramctl `setup` overwrites ~/.engram/.env to consolidation_profile=standard, recall_profile=all, integration=rework, worker=true and drops RUNTIME_ROLE/MCP_SURFACE/FASTEMBED lines — the config recorded as causing macOS Jetsam; it also lacks the `brain` subcommand | installed engramctl:357-403 vs installer/engramctl:48-50,230,393-402,1019-1050; CLAUDE.md 'standard+monolith caused Jetsam' |
| T1 | repair | Wrong-brain-by-default: an absent ENGRAM_HELIX__DATA_DIR opens ~/.helix/engram-native (440 KB May-26 store) and /health reports healthy on it | native/helix-repo/helix-python/src/lib.rs:39-45; diagnostics.py:56-58; config.py:3557-3561; ls ~/.helix/engram-native data.mdb 393216 B |
| T1 | repair | SessionStart hook POSTs /api/knowledge/bootstrap unconditionally and the route ignores artifact_bootstrap_enabled — 695 bootstrap rows + 546 Artifact entities re-seed any brain on first use and contaminate the small-brain question (P4) | ~/.engram/hooks/session-start.sh:95-105; api/knowledge.py:511-526 (no flag gate); project_bootstrap.py:33-54 skips only invalid_path; brain-status.json bootstrap_supersede.files=695 |
| T1 | repair | `engram captain export` opens the native graph in a second process with no exclusive-access guard — double-opens LMDB if the shell is up (the SIGBUS/leak hazard) | captain_cli.py:91-103; locks exist only in index_cli.py:70-73, cues_cli.py:104-110, main.py:137-143, mcp/server.py:324 |
| T1 | code-baggage | `engram setup` REQUIRES an Anthropic key on fresh install, writes it to .env and the MCP env block, and smoke-tests against the Anthropic API — contradicts the 09-04 resident-agent decision | setup.py:120-133 (_ask loops 'This field is required'), :325-340, :444-458; tests/test_setup.py:66-84,215-217,248-283 |
| T1 | code-baggage | Key-activated Gemini/Voyage embedding egress on provider='auto': a stray GEMINI_API_KEY/VOYAGE_API_KEY silently switches embeddings to a cloud provider (install pins local; convention only) | storage/factory.py:29-73 (load_dotenv side effect :29-33); embeddings/provider.py:83-158,318-540; config.py:115-124 default 'auto'; tests/test_local_embeddings.py:174-183 constructs VoyageProvider |
| T1 | repair | A spreading traversal cancelled by the Step 4 wall clock discards every completed read (k-1 finished reads thrown away) — needs a caller-owned frontier so it degrades to shallow, not to nothing | STANDING_GOAL.md:271; pipeline.py Step 4 asyncio.wait_for; probe test_a_discarded_traversal_still_reports_what_was_discarded |
| T1 | repair | Deletion of the duplicate pre-pipeline durable rescue still awaits Konner's §2.14 confirmation (working code deleted 09-02) | STANDING_GOAL.md:234 (d71dba7, 65f4230, 6b8bc3c); memory rule no-premature-deletion |
| T2 | repair | Machinery is STORED at capture (salience gate only withholds the vector) and is_machinery has no capture-time caller — 1,033 rows today, re-accumulates on any store | capture_service.py:239-243; is_machinery callers = pipeline.py:602, index_completeness.py:1103, evaluation/battery.py:55 (verified grep); all 1,033 rows source auto:prompt |
| T2 | design | Graph as memory structure inert on the shipped config: entity budget 0 discards every entity/working-memory/graph-structural score; scoreBreakdown 0.0 live; graph expansion costs ~77 ms/recall for 0 candidates; episode_graph_signal default-off with no profile setter; kill rig KILL | config.py:956,807; pipeline.py:2757-2762; candidate_pool.py:1001-1009,255-262; live GET /api/knowledge/recall stageTimingsMs graphExpandTimeout 76-77 ms, recallGraphCandidateCount 0; GRAPH_THESIS.md:502-529 |
| T2 | design | 11 of 15 consolidation phases have no runner on the shipped config (triage/merge/infer/compact/reindex/microglia/dream enabled-but-unscheduled; calibrate/reflect/immunity/reconsolidation dark-by-config); merge's mop exact-identifier slot ships OFF; 20/20 history cycles stuck 'running' | brain_cli.py:37-45,384-386; hygiene_ops.py:302-407,361-379; phase_registry.py:25-41; config.py:1463-1469,1871,3067,3053,3358; GET /api/consolidation/history |
| T2 | repair | Cue hygiene write side: demotion blanks cue_text but never drops the cue VECTOR; the native delete_cue_vector query exists (DROP V<CueVec>) and is registered in the route tuple but has NO Python wrapper or caller; 6,813 text-less cue nodes uncounted until this dossier | cue_hygiene.py:142 cue_text=''; schema.hx:730 QUERY delete_cue_vector; search.py:2364 route tuple; grep of engram/ for Python callers returns none (verified); GET /api/storage counts.cues 7,615 vs cue_metrics.cue_count 802 |
| T2 | old-brain-baggage | 7,017 episode vectors (81%) carry no readable float payload by id — MMR diversity, inhibitory spreading, state-dependent retrieval are no-ops on them; mop backfill at 50/window = ~23 days | search.py:2310-2325; STANDING_GOAL.md:292; brain-status.json vector_backfill.missing_before.episodes=7021; hygiene_ops.py:437-560 |
| T2 | repair | 1.18M ConsolDecisionTrace docs share the user-facing BM25 index (avgdl 70.7 vs episode mean 339.8, ~4.8x length penalty); no delete query in schema.hx; Python readers exist (audit_reader.py, learning.py:115, triage.py, phase_runner.py, presenter.py) — recurs on any store with every mop window | schema.hx:1167 (AddN) vs :757 (SearchBM25<Episode>), :1432 per-cycle read; STANDING_GOAL.md:276; judge 1 reader list |
| T2 | measurement | Usage tiebreaker ships OFF while its producer now fires (cue usedCount 48); ticket 7/37 gate not yet shown to move | config.py:2605-2616 usage_ranking_enabled False, no profile setter; GET /api/lifecycle/summary cue.usedCount=48; STANDING_GOAL.md:273,280 |
| T2 | repair | Durable-feeder 5 s TTL cache keyed on id(graph_store) can never hit across recalls (every recall wraps the store in a fresh GatedGraphStore); pool_entity_query_limit is the only pool limit that never scales | candidate_pool.py:316, :931; pipeline.py:871; STANDING_GOAL.md:270,272 |
| T2 | code-baggage | Triage LLM escalation flag defaults TRUE and quiet never flips it — held off only by os.environ ANTHROPIC_API_KEY at call time (the recurring silent-inert shape) | triage.py:233; config.py:1633-1636; quiet sets only judge False at :3269; standard-only no-key guard :3461-3467 |
| T2 | repair | Native engine debts: find_episode_by_episode_id is still a label scan with no INDEX; ef_search 768 is clamped to 512 (authority test pins the literal); whether the live shell runs the b2bf4cc arena-leak fix is UNKNOWN; Rust does not read config.hx.json | schema.hx:388; vector_core.rs:66 clamp(10,512); queries.rs:100; test_native_config_authority.py:96; STANDING_GOAL.md:210,237,253,277 |
| T2 | code-baggage | Dead knobs and orphans: UsageMeter constructed every boot, never read (record_llm_tokens 0 callers); HelixDBConfig.config_path zero readers while docker-compose.helix.yml sets it; EmbeddingConfig.hnsw_* Redis-only, hnsw_ef_runtime zero readers | security/usage.py:36-50; main.py:350,383,460; config.py:84,128-130; redis_search.py:73-74; STANDING_GOAL.md:212-213 |
| T2 | repair | Dashboard degraded under shell: POST /api/consolidation/trigger 409 by design; /api/consolidation/status serves a Jul-21 dry-run 'running' with scheduler_active=false; PressureAccumulator display-only; atlas relationship count sampled | api/consolidation.py:42-52; live GET /api/consolidation/status latest_cycle cyc_82032206bee9 started 2026-07-21 status 'running' completed_at set |
| T2 | repair | Packet-cache fingerprint omits recall_usage_surface_path (operator CLIs fingerprint differently from the server); pre-pipeline lanes present bare rows with no content/project (row 291, unverified in code) | packet_cache.py:55-61; STANDING_GOAL.md:281,291 |
| T2 | old-brain-baggage | Remaining old-brain junk in the graph: 6 throwaway Project entities, 9 'Engram' nodes (8 Concept, 5 empty summary), 5 'Cold Decision hit…' dupes, the misspelled sole Person-of-record 'Konnern Moshiere' (accessCount 0); the exact-merge slot cannot fold Concepts into the Project (blocks on entity_type) | GET /api/entities/search?q=Konner -> ent_dea20bfaa410; GET /api/stats topActivated 9x Engram; merge.py _exact_slice_candidates (judge 3); hygiene_ops.py:361-379 |
| T3 | design | Why Engram costs ~5x the characters of repo files (69k vs 13k for 8/14 vs 4/14) — §7 Q1 dense/current vs verbose/stale hypothesis untested; compression/recency presentation untouched | STANDING_GOAL.md:306,399-403; tests/rigs/agent_experience_battery_untouched_2026-09.json |
| T3 | design | Relationship yield from projected episodes is 0.0 (4,045 projected, 0 relationships); 40% of remember calls arrive without proposals (191/474); closed 34-predicate set undecided (Q3b); agent cooperation cannot be forced | GET /api/stats projection_metrics.yield.relationship_count=0; harness-metrics.json remember_without_proposals 191; promotion.py:37; STANDING_GOAL.md:284,409-425 |
| T3 | design | Adoption: the agent does not reach for memory unprompted (agentRecallCount 0 over a long session; contextLoadedThisSession false); P7 'should a session-start briefing exist' is a design experiment, not a filter | GET /api/knowledge/runtime agentAdoption; STANDING_GOAL.md:37-38,303 |
| T3 | design | Vocabulary-mismatch misses ('recall-outage' paraphrase; pair questions bound by their singles; conjunction split 0/3) — retrieval design, not baggage | STANDING_GOAL.md:244,246-247 |
| T3 | design | Small-brain / fresh-install behaviour (P4) and compaction-swap (P8, the only capability item) — unanswerable on the old brain; P4 needs a scratch store with hooks disabled | STANDING_GOAL.md:302,304,427-428; fresh reader: native store creatable today |
| T3 | code-baggage | Dashboard chat is the one live external-model path (anthropic.AsyncAnthropic per turn; fails into an SSE error frame without a key) and the sole reason `anthropic` is a base dependency — decide local alternative or drop, then move `anthropic` to a benchmark extra | chat_runtime.py:11,305-345; api/knowledge.py:42,732; ChatProvider.tsx:35; pyproject.toml:31; verified: AsyncAnthropic() constructs with api_key=None, fails at first messages.create |
| T3 | code-baggage | Dead external-model branches to delete after chat is decided: hyde, triage judge/escalation, infer Tier-3/escalation, merge LLM pass (keep _keep_separate_cache, fed by multi-signal path), edge server adjudicator, EntityExtractor class body (keep ExtractionResult/ExtractionStatus; swap lifecycle_cli.py:126 and authority_cli.py:97 to create_extractor; 4 _EmptyExtractor subclasses), Voyage/Gemini providers, Cohere reranker, standard-only no-key guard, LLMObservationSynthesizer stub | hyde.py:45; triage.py:53-83,233; infer.py:497-660; merge.py:1174-1264,692; edge_adjudication.py:121,279-330; extractor.py:66-80; provider.py:83-540; reranker.py:35-93; config.py:3461-3467; synthesizer.py:105-153 |
| T3 | code-baggage | Native Helix schema is compiled with a gemini server-side embedder (config.hx.json:14); Python provider deletions do not touch it; needs a rebuild | helixdb-cfg/config.hx.json:14 embedding_model 'gemini:…'; 7 Embed() sites in schema.hx (verified count, census said 13) |
| T3 | doc | Docs FALSE-NOW: README.md:264,274; SKILL.md:7,82 (clawhub v0.3.6 needs republish); docs/REFERENCE.md ~30 lines (:126-147,157-197,206,212-217,230,334-356,440,478,1055-1057,1577-1583,1661-1692,1783-1785,1807); docs/install/helix.md:18,134,146-148,228; docs/install/lite.md:127-128; .env.example:16-25,46-47; openclaw-distribution-strategy.md:161; hot-cold-process-split.md:373; gitignored AGENTS.md:25-27,56-58 and CLAUDE.md:205; website/src DocsPage.tsx:135-136, HomePage.tsx:70-71; code comments doctor.py:930, graph_manager.py:654, config.py:2175, mcp/server.py:1309 | protocol dossier; factory.py:1-30; config.py:2162-2170; context_builder.py:662 template briefing |
| T3 | design | Ticket 4 bundle (RF flip, ef_search rebuild targeting 512, reindex_sweep re-test, feeder flip, website sync) and key rotation for keys that were on disk (operator action) | STANDING_GOAL.md:305,277,210,625-628 |
| T4 | design | FalkorDB/Redis full lane (~3.8k lines, zero falkordb tests) and Postgres consolidation store (1,326 lines, unreachable on helix, set only by docs/SAAS_NEXT_STEPS.md:40,117): retire-or-keep is a lane decision needing a Docker boot falsifier, not part of the model sweep | storage/factory.py:232-290; resolver.py:44-58; bootstrap.py:134-150; CODE_CENSUS.md:421-426,619 |
| T4 | design | Projects as topology never evaluated (mechanism = 0.6 multiplier + tag); cross-device sync and federation absent by decision; profile matrix (567 Field(), ~140 *_enabled) collapse proposed by Plan 3 but deferred | pipeline.py:562-624; docs/product/MULTI_DEVICE_SYNC.md:1-4; GRAPH_THESIS.md:19-23; config.py (judge 1 count: 141 distinct *_enabled, not 279) |
| T4 | measurement | Operational unknowns: cold-brain battery-skip default on a laptop mostly on battery; cause of the 18-day shell outage (08-15 -> 09-02); why Sep-3 backups fell back to method='copy' (no clonefile) so every backup costs a real 7.6 GB | STANDING_GOAL.md:226,251; ~/.helix/backups manifests method='copy'; backup_cli.py:142-154 |

## Judges
- winner: **Fresh Brain, Narrow Loop (25) — wins on baggage removal and agent focus, not on measurement: on the measurement axis it ties Plan 2 at 6, and its central 'comparable meter after re-seed' claim is broken until created_at is carried through the seed path (capture_service.py:84,131 stamps utc_now(); recency scoring at pipeline.py:2278-2283 would flatten the corpus). Condition for the win to hold: amend step 4 to seed through a shell-down store-level script that sets created_at, and add a created_at-distribution probe; and every deletion step stays gated on Konner's explicit approval (reversibility 3). Runner-up Plan 2 (21) has the honest experimental design but a false cost premise (May-June 4,581 rows have no transcript copy — transcripts begin 2026-07-07). Plan 1 (19) keeps the baseline but bundles six interventions before one measurement and leans on a cue-vector delete function that does not exist.** — Keep the brain: purge, compact, correct,: 19, Fresh Brain, Same Product: retire the do: 21, Fresh Brain, Narrow Loop: 25
- winner: **Keep the brain: purge, compact, correct, re-measure — by one point (22 vs 21 vs 21), and on the persona tie-break it would win regardless: it is the only plan under which recall on the NEXT unseen question is worth any tokens, because the answer-bearing May–June rows exist nowhere else (Claude Code transcripts start 2026-07-07). Steal Plan 3's real content export before Plan 1's first delete, and Plan 2's 48-hour machine probe as the first thing to run after compaction.** — Keep the brain: purge, compact, correct,: 22, Fresh Brain, Same Product: retire the do: 21, Fresh Brain, Narrow Loop: 21
- winner: **Fresh Brain, Same Product: retire the dogfood store and let the resident agent rebuild it** — Keep the brain: purge, compact, correct,: 20, Fresh Brain, Same Product: retire the do: 25, Fresh Brain, Narrow Loop: 23

## Disagreements (not smoothed over)
- The three judges picked three different winners: judge 1 → Fresh Brain Narrow Loop (25/19/21), judge 2 → Keep the brain (22 vs 21 vs 21, on the 'next unseen question' tie-break), judge 3 → Fresh Brain Same Product (25 vs 20 vs 23). Aggregate totals are Narrow Loop 69, Same Product 67, Keep 61 — a 2-point spread with reversibility scores of 2-3 for the aggregate winner. This synthesis takes Plan 2's switch mechanics, Plan 3's export script and instrument-repeatability gate, and Plan 1's ids invariant + write-side fixes, and DEFERS Plan 3's code deletions; that is not any judge's winner verbatim.
- Keep vs fresh on the 'next unseen question' argument (judge 2): under Plan 2 the old content leaves recall for 14 days; under this synthesis it is re-seeded from the export, so judge 2's objection is met — but only if the re-seed carries project and conversation_date. Verified: ObserveBody has no project field, so the load-bearing project-prefix effect is at risk unless the seed path is resolved first; this stays a blocking unknown, not a smoothed-over detail.
- Whether re-seeding flattens recency: judge 1 says created_at is re-stamped (true: capture_service.py:84,131 utc_now) and recency scoring at pipeline.py:2278-2283 would treat every row as today; judge 2 says recency reads conversation_date. Verified: the recency lane reads ep.conversation_date (pipeline.py:2303-2305, 2332-2334, 2395-2397), so judge 2 is right for ranking; created_at still shifts keyset listing order and any created_at consumer, which is why the export must carry both fields.
- delete_cue_vector: judges 1 and 2 say no such function exists (Plan 1 step 5 needs new native code); the ledger row cites search.py:2364. Verified: it is a real native HelixQL query (schema.hx:730, DROP V<CueVec>) registered in the route tuple at search.py:2364, with no Python wrapper or caller. Both sides were half right; the fix is a thin wrapper, not new native code.
- Plan 1's claim that bootstrap_supersede is the proven purge path for all junk rows: judge 3 refuted it (it selects only stale bootstrap snapshots). Verified: bootstrap_supersede.py computes `stale = stale_snapshots(rows)` and purges only that list; the ~1,900 machinery/session/probe rows need a new driver over purge_episode/purge_episode_vectors/delete_entity.
- Plan 2's cost premise ('the only irreplaceable content is duplicated untruncated in 1,029 Claude Code transcripts, all dated after 2026-05-20'): refuted by all three judges and re-verified here — earliest transcript 2026-07-07 by mtime and in-file timestamp; May-June 4,581 rows exist only in the store. The sentence is literally true ('after 05-20') and materially misleading.
- Is cold 0/14 size (Plans 1 and 2) or code (the context_packet_fallback cascade + recent_observation hijack, Plan 3's fear)? Unresolved by any evidence in hand; step 6 (re-seeded small store, same instrument) is the decider and both outcomes are pre-registered.
- Whether the swap number on an empty store can be attributed to Engram: judge 1 notes shell RSS is ~1.3 GB of a 3.3-3.6 GB machine-wide swap with three claude processes, WebKit and ChatGPT resident; judge 3 accepts Plan 2's < 1.5 GB threshold as a genuine falsifier. Step 4 records other-process RSS beside the number; a 'machine, not store' verdict stays provisional until desktop load is comparable.
- Plan 3's structural code cuts: judge 1 scores them the winning move (baggage 9, agent 7); judges 2 and 3 score reversibility 2-3 and note probes that annotate failures instead of reversing cuts, the kill rig losing its arms and its lite backend, and delete-then-probe ordering against the no-premature-deletion rule. Deferred here to step 11 behind flag-off probes; not decided.
- Plan 1 step 8's merge probe ('exactly 1 Project Engram') cannot come from the exact-merge slot: judge 3 shows _exact_slice_candidates blocks on (entity_type, prefix), so the 8 Concept 'Engram' rows fold into each other, not into the Project. Folding needs a hand edit or a different mechanism.
- ConsolDecisionTrace deletion: Plan 1 and the ledger say the only reader is schema.hx:1432; judge 1 lists Python readers (audit_reader.py, presenter.py, learning.py, triage.py, phase_runner.py). Deletion needs a consumer check plus a new HelixQL query and native rebuild — unbudgeted in every plan.
- Postgres lane 'never set by any doc' (baggage census and CODE_CENSUS.md:426): refuted — docs/SAAS_NEXT_STEPS.md:40,117 sets ENGRAM_POSTGRES__DSN. Keep-vs-retire for FalkorDB/Redis/Postgres remains a lane decision with a Docker boot test as falsifier; not part of the model sweep.
- Plan 2's day-14 KEEP gate: judge 3 calls it near-certain to pass by construction (cold ≠ 0 on a RAM-resident store is what the plan predicts; yield > 0 passes on the first agent edge) and notes it omits the chars bar the plan names elsewhere. Step 9 here adds 'answers ≥ repo files at ≤ 2x chars on a second-agent question set' as a fourth condition.
- Judge 3 accepts Plan 1's compact --apply re-test (header repair + write probe, backup_cli.py:549-556) as the closing of row 238; memory still marks compact --apply UNSAFE from 09-02. Not exercised here (needs the shell down); the fresh path sidesteps it, so it is left open rather than resolved.

## The three plans (theses)
- **Keep the brain: purge, compact, correct, re-measure** — The 7.6 GB dogfood store is two things glued together: ~6,100 irreplaceable rows (about 5,550 real user/assistant turns across ten projects, 509 Codex summaries, 4 explicit mcp/user_correction captures, 239 conversation-derived entities with 404 ent-ent edges, and the only behavioural signal Engram has ever earned: 144 cues with 1,952 hits, 388 near-misses, 48 used-episodes) wrapped in ~2,600 regenerable or disposable rows (695 bootstrap snapshots, ~1,900 machinery/session/probe rows), ~610 bootstrap-derived Artifact/decision_statement entities carrying ~1,300 of the 1,761 edges, 6,813 text-less cue nodes, and 1.18 M consolidation audit traces sharing the user-facing BM25 index. Nothing in the first group can be re-exported: openapi has no /api/episodes/{id}, the listing caps content at 200 chars (graph.py:3167), captain export is name/summary-only and double-opens the store (captain_cli.py:91-103), and cue counters are written only by the recall pipeline (feedback.py:783-832). A fresh brain therefore is not "migrate the good part"; it is "lose the good part, lose the corpus every meter and battery number was taken on, re-inherit every stale ~/.engram sidecar by id anyway (activation-snapshot, hygiene offsets, harness-metrics), and get re-seeded with 695 bootstrap rows by the SessionStart hook on first use". Meanwhile every wrong thing in the second group is an ordinary repair with a tool that has already run on this store: bootstrap_supersede purged 3,386 episodes live via purge_episode/purge_episode_vectors (17ccd8f; graph.py:3015, search.py:2464), ovfix repaired the 16 SIGBUS cues, backup compact now carries header repair plus a write probe (backup_cli.py:549-556), and the mop already drains vectors, machinery, and usage decay every 2 h. The size-driven failures (swap 3.3/4 GB, 29 s single-key lookups, cold meter 0/14 via context_packet_fallback at 2.5-4.5 s, brain windows dying on 20 s native timeouts) are the case for shrinking the store in place, not for abandoning it; and the plan ends with a probe that can kill it: if cold recall is still 0/14 after purge + compact, the size hypothesis is wrong and the fresh-brain option gets its evidence for the price of one verified backup.
- **Fresh Brain, Same Product: retire the dogfood store and let the resident agent rebuild it** — Every number Engram uses to judge itself was measured on one store, and that store is confounded three ways at once. (1) It no longer fits the machine: 7.6 GB LMDB for 8,692 episodes (GET /api/storage paths[0].bytes), swap 3.39/4.0 GB used right now (sysctl vm.swapusage), native 20 s timeouts still killing brain runs on 09-04 (~/.engram/logs/engram-brain.log 08:48), and the meter's mid-run collapse was diagnosed as cold pages, not ranking (STANDING_GOAL.md:223,244). (2) Its corpus is mostly not memory: at least 1,033 tag-shaped harness rows, 587 session markers, 249 probe rows, 33 compaction markers, 695 regenerable bootstrap snapshots (~22% non-conversational, brain census); 7,017 of 8,692 episode vectors (81%) have no readable payload (STANDING_GOAL.md:292; search.py:2310-2325); 6,813 of 7,615 cue nodes carry no text (GET /api/storage counts.cues vs /api/stats cue_metrics.cue_count); 1.18 M ConsolDecisionTrace docs sit in the user-facing BM25 index dragging avgdl to 70.7 vs 339.8 (STANDING_GOAL.md:276). Its graph is not the graph the product promises: 546/953 entities are file-path Artifacts, ~87% of reachable entities classify as junk by name, 'Engram' exists 9 times, the only Person-of-record is the misspelled 'Konnern Moshiere', 77% of edges are bootstrap/decision_statement chains, and projected episodes yield 0.0 relationships (GET /api/stats projection_metrics.yield.avg_relationships_per_projected_episode = 0.0 over 4,045 projected). (3) It is self-referential and contaminated: it holds this session's own analyses of the rig questions (STANDING_GOAL.md:249) and pytest wrote into its live harness scoreboard until 09-04 (STANDING_GOAL.md:285). Meanwhile the product changed today: the resident agent is the only extractor, agent edges land on every profile, every episode is stamped extractedBy, vectors are written at capture, project is set at capture (a18dd58, 2b9ddbf, 4907143, 91ded55). The old brain cannot show what that product does, because its 4,045 projected rows predate all of it and read as narrow. A fresh store is the one experiment that separates 'the product' from 'the accumulated store', and it is cheap: the only irreplaceable content, the ~6,500 real conversation turns, is duplicated untruncated in 1,029 Claude Code transcripts (~/.claude/projects, 626 MB, all dated after 2026-05-20) and 2,005 Codex session files; the old data dir stays on disk as its own full-fidelity archive (no REST route exports full episode content: openapi has only GET /api/episodes, capped at content[:200], graph.py:3167); and reversal is one line in ~/.engram/.env. The strongest case is not that the old brain is bad data; it is that nothing measured on it can be attributed to the product, and the fresh brain's own falsifier is built in: if an empty store still swaps and still times out, the machine was the problem and the .env line goes back.
- **Fresh Brain, Narrow Loop** — The only loop with a measured consumer is capture -> (episode vector + BM25 + cue) -> RRF recall -> the resident agent proposes structure. A live recall today (GET /api/knowledge/recall, budget.durationMs 269, status ok) scores 4/4 items on `semantic` alone with activation/spreading/edgeProximity = 0.0, spends 77.5 ms (graphExpandTimeout) on a graph expansion that returns 0 candidates (recallGraphCandidateCount 0, recallGraphGateRefusals 30, recallSpreadInjected 0, recallWorkingMemoryCandidateCount 0), and the kill rig scored 0/51 with and without the graph (GRAPH_THESIS.md §5). Everything outside the loop is dark-by-config, has no runner (11 of 15 phases; GET /api/consolidation/history: 20/20 cycles stuck 'running'), computes output that is discarded (working memory, graph embeddings, the entity channel at passage_first_entity_budget=0), or has never booted here (FalkorDB/Redis/Postgres, Anthropic chat). The old brain turns every measurement into a swap-regime measurement: 7.6 GB for 8.7k episodes, >=11.9% tag-shaped machinery rows, 64% of entities and 77% of edges regenerable bootstrap junk, 7,017 episode vectors with no readable payload, 6,813 empty cue nodes, 1.18M audit docs dragging BM25 avgdl to 70.7 vs episode mean 339.8, cold runs 0/14. The strongest case for subtraction is not that these mechanisms are wrong; it is that each is a question nobody can answer while it sits on the hot path or in config.py (567 Field() definitions, 279 *_enabled flags, ~30 never-set gates), and answers are the product. Order matters: (1) fresh store with UNCHANGED code so the store variable is isolated (§2.6 one machine at a time); (2) cut code to the loop one mechanism class per probe, measured on the fresh brain where the meter is trustworthy; (3) grow only what a probe re-admits. Every cut keeps its falsifier: the 14 unseen-question meter (3-4/14 warm, 0 cold today), the untouched agent battery (8/14 vs 4/14 files at 69k vs 13k chars), 10 consecutive explicit recalls, shell RSS/swap.
