# engram (server package)

This is the Python package behind Engram, the local memory layer for AI agents.
The project README, install guide and docs live one directory up: `../README.md`, `../docs/`.
