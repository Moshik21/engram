TASK D — GEAR TRIAGE (all claims re-verified in code this session; sibling diff read, not touched)

| # | Gear | Evidence (file:line, verified) | Verdict | Owner | Est. size |
|---|------|-------------------------------|---------|-------|-----------|
| 1 | TS positional Beta draws / TS existence | `retrieval/scorer.py:231` (`rng = random.Random(rng_seed)`), `:265-266` (one `rng.betavariate` per candidate, list order). Seed = `blake2b(group\x00query)` at `pipeline.py:1639-1646` — sample→entity binding is only as stable as pool composition (M4.3: 0/36 repeat-stable vs 33/36 off). Learning loop: read-path feedback only via `pipeline.py:2140-2157` (M1.2 guard ⇒ dead at shipped budget 0); posterior writers `activation/feedback.py:9-32` write only `ts_alpha/ts_beta` (`models/activation.py:18-19`); the sole other caller is the dashboard chat surface (`retrieval/feedback.py:563-571` ← `chat_runtime.py:15`) — no MCP/agent-surface reward signal exists, and M4.4 (the signal source) is PARKED with no source in sight. | **KILL** — a bandit whose reward channel is structurally absent is not eval-gateable; at budget 0 doubly inert, at budget >0 it buys −1@5/−1@10 and costs 33→0 repeat-stability. Delete: `score_candidates_thompson` (scorer.py:209-~330), pipeline branch 1639-1646 + step 7 2140-2157, `activation/feedback.py`, `ts_*` knobs (config.py:366-369), `ts_alpha/ts_beta` fields + snapshot compat, chat_feedback TS hooks. NOTE: supersedes recorded DECISIONS-2026-07-17 "flip parked" — needs founder ack; floor if ack withheld: one-line `ts_enabled=False` default + test (mechanism-justified, corpus-independent). If TS ever returns: per-entity seeds `blake2b(group,query,entity_id)`, M4.1 rig as gate. | now (founder ack) | kill ~1 day incl. tests; floor flip ~1 hr |
| 2 | Predicate allowlist silent-cap | Gate chain: `extraction/client_proposals.py:253-262` (free-form predicate → conf capped 0.40 + `predicate_not_allowed` signal) → `extraction/commit_policy.py:126-133` (hard reject). Surfacing today: counters only (`extraction/harness_metrics.py:183-207`, `ingestion/projection_execution.py:264`); agent-visible error ONLY when zero proposals commit (`ingestion/capture_surface.py:1301-1312`); partial-commit case (M3.1's exact failure: entities commit, all edges silently rejected → 0-relationship brain) reports bare counts, no reasons; rejected evidence rows never serialized (projection_execution.py:274-287 serializes committed+deferred only). **New confirmed sub-defect:** gate checks the PRE-canonical form but apply canonicalizes at commit (`extraction/apply.py:346-356`): `LIVES_IN` passes then stores as `LOCATED_IN`, while proposing `LOCATED_IN` (the vocabulary actually in the graph) is REJECTED; same for `DEPENDS_ON`/`BLOCKED_BY`→`REQUIRES` (`canonicalize.py:34,100-102` vs `promotion.py:33-56`) — allowlist not closed under canonicalization, in both directions. | **FIX-NOW**, 3-part stack: (a) canonicalize via `PredicateCanonicalizer` before the allowlist check (client_proposals.py:242); (b) make `ALLOWED_CLIENT_PREDICATES` canonical-closed (add canonical targets: LOCATED_IN, REQUIRES, HAS_ROLE, CREATED, LIKES, DISLIKES, AIMS_FOR, EXPERT_IN, COLLABORATES_WITH, LEADS); (c) reject LOUDLY: serialize rejected rows (status="rejected") and emit `harness_extractor.rejected_relationships:[{subject,predicate,object,reason}]` + the allowlist in the error payload, on partial commits too. Do NOT invent synonym mappings (INTERESTED_IN stays rejected — but visibly, so the agent retries with PREFERS). Tests: LOCATED_IN commits; LIVES_IN and stored predicate agree; partial-commit response carries per-edge reasons. **Coordinate: sibling `fix-commit-policy` is active on this file (tree currently clean — message them before landing).** | now | 0.5-1 day |
| 3 | seed_threshold post-scale-fix | Sibling's in-flight diff (read-only): pipeline.py adds Step 1.9 rescale of `candidates` to true cosine via `compute_similarity`, gated on `entity_episode_traversal_source=="candidates"` (default `"results"`, config.py:589), before `identify_seeds` at pipeline.py:1179-1187 — so under the flag, `spreading.py:42-45` finally compares cosine vs cosine. **Remains misaligned (4 items):** (a) `candidate_pool.py:727` graph-neighborhood seeds still filter RRF rank-normalized `search_results` ≥ `seed_threshold` (config.py:182) — still a rank cutoff; untouched by the diff (stat: pipeline/context_builder/graph_manager only); (b) threshold VALUE 0.3 never recalibrated for the cosine scale — review §3 puts a good neighbor at ~0.4, so 0.3-vs-cosine still seeds nearly everything; (c) `temporal_mode` (pipeline.py:485) zeroes the threshold (spreading.py:42) ⇒ every candidate seeds ⇒ scorer edge_prox=1.0 uniformly (scorer.py:254-255) — edge_proximity stays a constant on ALL temporal routes regardless of the scale fix; (d) default path (flag off) keeps the mixed scale entirely — accepted per eval-gating but means "seed_threshold semantics" are flag-dependent. | **FOLD-INTO-REDESIGN** — belongs to the M3.1(b) candidates-flip stack (Task C retrieval-surfacing section): recalibrate seed_threshold on the M3.1/M4.1 rig, unify or rename the candidate_pool.py:727 usage ("top-N rank picker", separate knob), and decide the temporal-mode edge_prox contract. No independent fix while the sibling owns these files. | redesign — retrieval-surfacing section | 0.5 day + rig re-run |
| 4 | Illusory config ablation (router deep-copy) | `retrieval/router.py:100-108`: `apply_route` deep-copies cfg and unconditionally overwrites weight_semantic/activation/spreading/edge_proximity from `_WEIGHT_PROFILES` (router.py:49-56) — a config-level `weight_activation=0` silently does nothing on every routed query (voided M4.1's first D arm; DECISIONS caveat documents but doesn't fix). Not in the sibling's diff; not claimed by any experiment stack I can find (no Task C doc exists in docs/product/ — grep empty). | **FIX-NOW**: explicit-zero kill-switch — in `apply_route`, any weight the base cfg sets to exactly 0.0 stays 0.0 (zero = term disabled; profiles redistribute only among enabled terms, no renormalization). Behavior-preserving for every shipped config (no default is 0). Test: `cfg.weight_activation=0` + TEMPORAL query ⇒ `routed.weight_activation == 0`; default cfg ⇒ profiles unchanged byte-identical. Makes all future ablations (incl. M4.1's parked router-zeroing gate) honest via config. | now (unless Task C §3 claims routing — doc not found in repo) | ~1 hr |
| 5a | FastEmbed cache `model.onnx` (M3.1 incidental 2) | Already fixed in tree: stable cache dir `~/.engram/models/fastembed` replacing purgeable `$TMPDIR` (`embeddings/provider.py:16-32`), load failure logs ERROR + 300s broken-latch that self-heals on cache repair (provider.py:172-221), and `engram doctor` live-probes the real provider with an actual embed (doctor.py:753-798). Detection + healing exist; failure is loud. | **KILL** (no machinery to build — incidental already resolved; the only residual is that recall carries no per-request degraded-embeddings marker, which is review-§3 hot-path territory, not this note) | kill | 0 |
| 5b | `record_access=True` no-op at budget 0 + episodes never record + rich-get-richer loop (M4.1 structural finding 1) | `retrieval/primary_results.py:285` — access recorded only for surfaced entity results inside `if record_access:`; shipped `passage_first_entity_budget=0` ⇒ 0 events in 36 recalls (arm B0); episodes have no recording path at all. | **FOLD-INTO-REDESIGN** — owned by M4.4 confirmed-use design note (DECISIONS: reuse `retrieval/feedback.py` confirmed/corrected writer, which I verified exists and is live only on the chat surface). Correctly PARKED: implementing now would record ~0 events = new silent-inert. | redesign — M4.4 | parked |
| 5c | Sigmoid saturation + TEMPORAL profile 0.55 (M4.1 mechanisms) | `router.py:51` TEMPORAL `(0.20, 0.55, 0.15, 0.10)` — activation 2.75:1 over semantics; saturation constants in activation engine (1 access ⇒ 0.76-0.91). | **FOLD-INTO-REDESIGN** — these are two of the three recorded re-admission gates for activation-as-ranker (DECISIONS M4.1); flipping now would violate the parked real-corpus rerun. Gate rig: scratchpad/experiments/m41_activation arms A/B/E. | redesign — M4.1 re-admission | parked |
| 5d | Benchmark blindspot (`record_access=False` pins the north-star) | Proven moot by M4.1: empty in-process activation store ⇒ `base_act=0` ∀ candidates ⇒ A ≡ C bit-identical — the benchmark provably cannot credit or debit activation. | **KILL** as a defect (it's an accidental flattering no-op, documented); the "one eval arm with activation live" instrumentation is already folded into the M4.1 re-admission gate. | kill | 0 |
| 5e | `ts_enabled=False` also disables usage feedback (M4.3 incidental 3) | pipeline.py:2140 (`if cfg.ts_enabled and record_feedback`). Intended pairing per M4.3. | **KILL** — subsumed by gear 1 (whole loop deleted); if gear-1 floor (flip only) is taken instead, pairing is still correct as-is. | kill (w/ gear 1) | 0 |
| 5f | Session priming pollution / pool 56→61 mutation on read (M4.3 incidental 2) | — | SKIPPED per task instruction (Task B owns it) | Task B | — |

Cross-cutting notes for the caller: (1) gear 2 and the active `fix-commit-policy` sibling risk collision — extraction/ tree is clean right now but their scope overlaps commit_policy.py:126-133; (2) gear 1 KILL formally supersedes DECISIONS_2026-07-17's "flip parked" and GOAL decision 3 "SEEDED-ON" — do not land without founder ack; the 1-line default-off floor is safe immediately; (3) no Task C document exists under docs/product/ yet (grep for "Task C" empty), so gears 3/5b/5c ownership assignments are by section-topic, pending that doc's section numbering; (4) the sibling's Step 1.9 rescale is correctly gated and degrade-safe (timeout → mixed scale, not recall death) — no defect found in their in-flight diff itself.